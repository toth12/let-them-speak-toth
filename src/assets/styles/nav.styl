/**
* Nav
**/

nav {
  background-color: #000;
  opacity: 1;
  text-align: center;
  position: absolute;
  top: 0;
  right: 0;
  left: 0;
  z-index: 2;
  max-height: 75px;
  max-width: 100%;
  overflow: auto;
}

nav ul {
  display: inline-block;
  white-space: nowrap;
}

nav li {
  display: inline-block;
  padding: 30px 20px;
  vertical-align: top;
}

nav li.brand {
  padding: 12px 20px 0;
  width: 250px;
}

nav a {
  color: #A8A8A8;
  text-decoration: none;
  font-family: 'arno-pro', serif;
  letter-spacing: 1px;
}

nav a:hover {
  color: #ca8a17;
}
